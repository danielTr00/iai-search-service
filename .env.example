# Generate with: openssl rand -hex 32
SEARCH_API_TOKEN=
# Bound to loopback on the Spark host; the agent backend reaches it through host.docker.internal.
SEARCH_PORT=8090
SEARCH_MAX_CONCURRENT_REQUESTS=2
SEARCH_API_MEMORY=512m
SEARCH_API_CPUS=0.75
SEARXNG_MEMORY=768m
SEARXNG_CPUS=1.0
